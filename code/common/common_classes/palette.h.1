//LIC

#ifndef PALETTE_H
#define PALETTE_H

#include <QString>
#include <QColor>
#include <vector>

namespace _palette_ns {

  enum class _palette_type:unsigned char {PALETTE_TYPE_DISCRETE, PALETTE_TYPE_CONTINUOUS};
  const _palette_type PALETTE_TYPE=_palette_type::PALETTE_TYPE_DISCRETE;

  enum class _palette_color_type:unsigned char {PALETTE_COLOR_TYPE_SINGLE_TONE, PALETTE_COLOR_TYPE_COLOR};
  const _palette_color_type PALETTE_COLOR_TYPE=_palette_color_type::PALETTE_COLOR_TYPE_SINGLE_TONE;

  enum class _palette_color_assigning_type:unsigned char {PALETTE_COLOR_ASSIGNING_MUTABLE, PALETTE_COLOR_ASSIGNING_FIXED};
  const _palette_color_assigning_type PALETTE_COLOR_ASSIGNING=_palette_color_assigning_type::PALETTE_COLOR_ASSIGNING_MUTABLE;

  const int Palette_discrete_minimum_intervals=2;
  const int Palette_discrete_maximum_intervals=10;
  const int Palette_continious_minimum_intervals=1;
  const int Palette_continious_maximum_intervals=10;

  const QColor MUTABLE_COLOR=Qt::red;

  struct _palette {
    _palette_type Type;
    _palette_color_type Color_type;
    int Num_intervals;
    _palette_color_assigning_type Color_assigning;
    QColor Color;
    int Fixed;
  };

}

#endif


